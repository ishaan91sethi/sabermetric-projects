# Applies 10 fold CV with a few assumptions. First, each observation in the given dataframe has an assigned
# fold number in the "fold" column. Secondly, the fold that is left out is given as the second argument.

ten_fold_cv <- function(df, fold)



